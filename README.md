# Umpire-Decision-System
This project aims to assist cricket umpires by automatically classifying match events from images using Deep Learning. It is built using Convolutional Neural Networks (CNN) and trained on a labeled image dataset.
